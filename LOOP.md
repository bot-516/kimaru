# 自動ループの指示書

この内容を、リポジトリのフォルダで起動した Claude Code にそのまま渡す。

---

## 渡す文（ここから下をコピーして貼る）

まず `HANDOFF.md` を最後まで読んでください。このプロジェクトの前提・禁止事項・
過去に踏んだ罠が全部書いてあります。読まずに始めると同じ穴に落ちます。

そのうえで、以下のループを **10周** 回してください。私は寝ているので、
途中で質問せず、判断は自分でしてください。判断に迷ったら「変更しない」を選んでください。

### 1周の手順

**① 評価（別の目で見る）**

サブエージェントを3体、並列で立ててください。それぞれ **index.html を読むだけ** で、
直してはいけません。役割を分けます。

- A: 壊れ方を探す係。通信が切れたら／同時に触られたら／想定外の入力が来たら
  どうなるかを、コードを追って具体的に指摘する。
- B: 使う人の目線で見る係。初めて開いた人が迷う箇所、次に何をすればいいか
  分からない箇所、表示と実態が食い違う箇所を挙げる。
- C: 前周の直しを疑う係。直前のコミットの差分を読み、その変更が別のところを
  壊していないか、副作用が無いかだけを見る。（1周目は飛ばす）

それぞれ「ファイル名・行番号・何が起きるか・どういう条件で起きるか」の形で報告させて
ください。**再現条件を書けない指摘は採用しないでください。**

**② 選ぶ**

出てきた指摘を「起きやすさ × 実害」で並べ、**上位1件だけ**を選びます。
一度に何件も直すと、どれが原因で壊れたのか分からなくなります。

以下は直さないでください：
- `HANDOFF.md` の「絶対に壊してはいけない決まりごと」に反する変更
- 単一HTMLをやめる、フレームワークを入れる、ファイルを分割する
- 新機能の追加（このループは修正専用です）
- デザインの方針変更（配色・書体・絵文字・角丸）

**③ 直す**

選んだ1件を直します。直す前に、その不具合を**再現するテスト**を `tests/` に足して
ください。落ちることを確認してから直す。直したら通ることを確認する。

**④ 確認**

```
node tests/audit.js
node tests/bugcheck.js
node tests/testclick.js
node tests/testfail.js
node tests/testrace.js
node tests/testreport.js
node tests/testgroup.js
node tests/testgcal.js
node tests/testrecent.js
node tests/testfallback.js
```

- `audit.js` が **51/51 でなければ、その変更を捨てて次の周に進んでください。**
- どれか1本でも「エラー: なし」以外を出したら、同じく捨ててください。
- 全部通ったときだけ⑤に進みます。

**⑤ 記録して送る**

```bash
git add -A
git commit -m "<何を直したか>"
git push
```

**1周ごとに1コミット**にしてください。まとめないでください。あとで1周分だけ
取り消せるようにするためです。

コミットメッセージは日本語で、「何が起きていたか」を1行で書いてください。
例：`同時に候補日を足すと片方が消えるのを直した`

**⑥ 次の周へ**

3周続けて「直す価値のある指摘が出なかった」場合は、そこで終了してください。
無理に直すところを探さないでください。

### 終わったら

`LOOP_REPORT.md` を書いて、これもコミットしてください。中身は各周について：

- 何が指摘されたか
- どれを選んで、なぜそれを選んだか
- どう直したか
- 捨てた変更があれば、何を試して何が落ちたか
- 結局直さなかった指摘と、その理由

**直せなかったこと・捨てたことを省略しないでください。** 朝それを読んで、
次に何をするかを決めます。うまくいったことだけ書かれた報告は役に立ちません。

### やってはいけないこと

- `index.html` の Supabase の設定値を書き換える（キーもURLも触らない）
- 本物のSupabaseに繋いでテストする（テストは偽サーバで動きます。実データを汚さない）
- `main` 以外のブランチを作る、force push する、履歴を書き換える
- テストが落ちているのに push する
- 私に質問を投げて止まる（朝まで誰も答えません。自分で決めて、理由を記録してください）

---

## 事前に一度だけやること

```bash
git clone https://github.com/bot-516/kimaru.git
cd kimaru
npm install playwright
npx playwright install chromium
node tests/audit.js      # 51/51 が出れば準備完了
```

`git push` で認証を聞かれる場合は、寝る前に一度手で push して通しておくこと。
夜中に認証待ちで止まると、そこでループが終わります。
