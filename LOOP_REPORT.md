# 夜間10周ループ 実施報告

`HANDOFF.md` と `LOOP.md` の指示に従い、サブエージェント3体（A: 壊れ方探し／B: UX監査／
C: 前周diffの副作用チェック）を毎周並列起動し、指摘を「起きやすさ×実害」で比較して
上位1件だけを直す、というループを10周回した。全周、`node tests/audit.js` が51/51、
既存の全テストスイートが「エラー: なし」/「判定: OK」になったことを確認してからコミット・
pushしている。push失敗は一度も無かった（3周連続失敗時の中断ルールは発動していない）。

10周とも「直す価値のある指摘なし」にはならず、最後まで具体的な修正対象が見つかり続けた
ため、10周でループを終了する（早期終了はしていない）。

---

## ループ1

**指摘**（Agent A: 壊れ方／Agent B: UX。1周目のためAgent Cは省略）
- A: `createGroup()` が `db.from('groups').insert(...)` の戻り値の `error` を見ておらず、
  supabase-jsは失敗しても例外を投げないことがある（RLS拒否・制約違反・5xx等）ため、
  作成が失敗しても「できました」と表示され、無効な招待URLが配られてしまう
- A: `persist()`／`saveAnswer()`／`addMember()` にも同様の未チェック箇所がある
- B: グループ画面の「進行中」カードが締切超過後も「あと0時間」で固まる（`pullGroup()`が
  `tick()`を呼ばない）／確定後の再調整ボタンが警告文のすぐ下に置かれている 等

**選定と理由**：Aのトップ指摘（`createGroup()`のエラー握りつぶし）。HANDOFF「保存に失敗
したら『できました』と言わない」に直接反する、最も基本的な導線（グループ作成）の不具合。

**直し方**：`insert()`の戻り値から`error`を取り出し、あれば`throw`して既存のcatchブロック
（入力保持・失敗表示）に流す。再現テスト `tests/testerr.js` を追加。

**捨てた変更**：なし。

---

## ループ2

**指摘**：A/B/Cを並列起動。Cはループ1の修正の副作用を確認し「問題なし」と報告。
Aのトップ指摘は `persist()` が同じパターン（`.error`未チェック）を持ち、しかもアプリ内で
最も呼び出し頻度の高い書き込み経路（締切変更・候補日変更・確定処理すべて経由）である点。

**選定と理由**：Aのトップ指摘。同じバグでもcreateGroup()より影響範囲が広く、`lastRow`が
誤って進んでしまうと失敗した書き込みが二度と再送されない、という致命的な性質もあった。

**直し方**：`persist()`の`update()`の`error`をチェックし、エラー時は`lastRow`を進めない
（＝次回の`persist()`で差分として再送される）ようにした。`tests/testpersisterr.js`を追加。

**捨てた変更**：なし。

---

## ループ3

**指摘**：Cはループ2の修正に問題なしと報告。
- A: `boot()`の初回`pull()`/`pullGroup()`も同じ未チェックパターンを持ち、しかも「招待リンクを
  開いた直後」という最も発生頻度の高い箇所で、一度の通信エラーが「存在しない」と混同され
  URLハッシュが消されてしまう
- B: 締切フィールドに過去日時を入れても警告なく即座に確定してしまう（`closeNow()`の
  二段階確認と対照的）

**選定と理由**：Aのトップ指摘。招待リンクの初回オープンは全ユーザーが必ず通る経路であり、
モバイル回線での一時的な失敗は日常的に起きうる。失敗＝リンク切れという誤解は致命的。

**直し方**：`bootPull()`ヘルパーを追加し、初回読み込みを最大3回・400ms間隔でリトライして
から「見つからない」と判断するようにした。`tests/testcoldload.js`を追加。

**捨てた変更**：なし。

---

## ループ4

**指摘**：Cはループ3の修正に問題なしと報告。
- A: `joinGroup()`が`addMember()`失敗時にロールバックしておらず、失敗した参加が
  ローカルの`state.gmembers`に残ったまま新しい日程を作ると、サーバーに存在しない
  「幽霊メンバー」がその日程の参加者として焼き込まれる
- B: ホスト以外にもカレンダーの候補日セルや再調整ボタンがクリック可能に見える（実際は
  トーストで弾かれるだけ）等

**選定と理由**：Aのトップ指摘。ループ1の`createGroup()`修正と同じ構造のバグの「参加」版。
`addMember()`のフォールバック経路（配列書き込み）の`error`未チェックも同時に見つかり、
これも直さないとロールバック自体が機能しないため合わせて修正した。

**直し方**：`joinGroup()`に`isNew`判定とロールバック（失敗時に`state.gmembers`から除去、
`state.me`を設定しない）を追加。`addMember()`のフォールバックの`groups.update()`にも
`error`チェックと`throw`を追加。`tests/testjoinerr.js`を追加。

**捨てた変更**：なし。ただしテスト作成時に何度か手直しが必要だった（下記「テスト側の
不具合」参照）。

---

## ループ5

**指摘**：サブエージェント3体、1回目は全員インフラ都合（stall／API接続断）で失敗し、
全員を再実行した。
- A: `pull()`が`fromRow()`で毎回サーバー値を無条件上書きしているため、`persist()`の
  再送安全網（ループ2で追加）が「保存失敗の直後に3秒ごとの定期同期が割り込む」タイミング
  で無効化され、編集内容がサーバーの古い値で上書きされて消える
- B: グループ作成直後の「URLを貼ってください」トーストに反して、共有ボタンが画面最下部に
  あり見つけにくい 等

**選定と理由**：Aのトップ指摘。ループ2で作った安全網そのものを無効化してしまう、かなり
根本的な問題だった。

**直し方**：`pull()`に「`lastRow`と現在の`toRow()`が食い違う＝未確定の編集がある」場合の
ガードを追加し、その場合は`fromRow()`で上書きせず、先に`persist()`を再試行するようにした。
`tests/testeditloss.js`を追加。

**捨てた変更**：なし。

---

## ループ6

**指摘**：Cがループ5の修正の副作用を精査し、**実際に再現できる回帰を発見**：
`lastRow`/`lastSig`はイベントをまたぐモジュール変数であり、`openEvent()`や`backToGroup()`
でイベントを切り替えても初期化されない。そのため「イベントAを作る→グループに戻る→
イベントBを作る→戻る→イベントAを開き直す」という普通の操作で、ループ5のガードが
誤発火し、Bの未保存内容がAのDB行に書き込まれてしまうことをPlaywrightで実証した。
A/Bからも別の指摘（`state.seq`未永続化によるID衝突、`addMember()`の未チェックエラー等）
があったが、自分たちの直前の修正が引き起こした回帰を優先した。

**選定と理由**：C（前周diff監査）の指摘。前周の修正がバグを直した代わりに新しいバグを
生んでいたため、最優先で塞ぐべきと判断した。

**直し方**：ループ5のガード条件に`lastRow.id===eventId`を追加し、`lastRow`が別イベントの
ものである場合はガードを発火させず、素直に`fromRow()`で読み込むようにした。
`tests/testeventswitch.js`を追加（テストの組み立てに何度か手直しが必要だった。詳細は
下記「テスト側の不具合」参照）。

**捨てた変更**：なし。

---

## ループ7

**指摘**：Cがループ6の修正を検証し、**「方向としては正しいが完全ではない」**と報告：
`persist()`の完了時代入（`lastRow = row`）自体が`eventId`の一致を見ておらず、遅延した
`persist()`がイベント切り替え後に完了すると、id一致の偶然によって同じ穴が狭い条件下で
再現しうる（提案：完了時代入自体をガードする、または`openEvent()`/`backToGroup()`で
`lastRow`をリセットする）。
- A: `submitAnswer()`のローカル確定と、締切前後で走る`pull()`の`answers`読み込みが競合し、
  締切間際に送信した回答が確定処理から漏れて、実際には保存されたのに集計されないまま
  確定してしまう可能性
- B: `copyText()`が`execCommand('copy')`と`navigator.clipboard.writeText()`の結果を
  一切見ておらず、両方失敗しても「コピーしました」と表示する（LINEアプリ内ブラウザ等、
  想定される主要な利用環境で起きうる）

**選定と理由**：C・Aの指摘は魅力的だったが、いずれも`pull()`/`persist()`/`lastRow`という
既に3周連続で手を入れている複雑な状態管理にさらに手を加えるもので、意図せず新しい穴を
開けるリスクが高いと判断した。Bの指摘は純粋にDOM操作だけで完結する低リスクな修正であり、
かつ「招待URLを共有できる」というアプリの唯一の配布経路を直接壊しているため選んだ。

**直し方**：`copyText()`を、`execCommand('copy')`の戻り値（真偽値）を確認し、失敗時のみ
`navigator.clipboard.writeText()`を正しく`await`・`catch`して結果を見てからトーストを
出すように書き換えた。`tests/testcopyfail.js`を追加。

**捨てた変更**：なし。

**直さなかった指摘とその理由**（この周で見送ったもの）：
- C発見の残存する狭い競合（`persist()`完了時代入の`eventId`未チェック）：発生条件が
  「遅延した保存＋その間に別イベントへ切り替えて戻る」という狭いタイミングで、`pull()`側の
  ガード（ループ6で追加）と併せて既にリスクはかなり下がっている。10周という限られた
  ループの中で同じ箇所に何度も手を入れ続けるより、他の箇所の既知の不具合を優先すべきと
  判断した。**未解決のまま残っている。**
- A発見の`submitAnswer()`/`pull()`競合（締切間際の回答が確定処理から漏れる可能性）：
  同上の理由に加え、修正には`state.answers`側にもループ5相当の「保留中の編集」概念を
  新設する必要があり、影響範囲の見積もりに時間がかかると判断した。**未解決のまま残っている。**

---

## ループ8

**指摘**：Cはループ7の修正（`copyText()`）を検証し、ほぼ問題なしと報告。ただし1点、
「同時に表示されている2つの異なるコピーボタンを素早く連打すると、非同期処理の解決順序
次第で違うボタンの成功メッセージが表示されうる」という軽微な指摘があった（データ損失は
なし）。
- A: `setSlot()`（開始・終了時刻の変更）だけ、兄弟関数の`addCandidate()`/`removeCandidate()`
  と違い`state.confirmed`のチェックが無く、**確定済みの日程の時刻をフィードにも残さず
  無警告で書き換えられる**
- B: 締切が候補日より後に設定されていても警告が無く、過ぎた候補日も「過去」の見た目に
  ならないため、既に終わった日を自信満々に確定として発表しうる 等

**選定と理由**：Aのトップ指摘。通信エラーや競合のタイミングに一切依存せず、ホストが
普通に「設定」から時間を少し調整するだけで100%再現する、確定情報の無断書き換えだった。

**直し方**：`setSlot()`に`needHost()`と`state.confirmed`のガードを追加し、兄弟関数と
同じパターンに揃えた。`tests/testconfirmedslot.js`を追加。

**直さなかった指摘とその理由**：Cの「2つのコピーボタン連打時のトースト文言の競合」は、
発生条件が限定的（実行環境がexecCommand失敗＋clipboard非同期に該当し、かつ2つの
コピー操作をほぼ同時に行う）でデータ損失を伴わない表示上の問題のため、見送った。
**未解決のまま残っている。**

---

## ループ9

**指摘**：Cはループ8の修正（`setSlot()`）を検証し、「隠されていない、実際に到達可能な
穴を正しく塞いでおり、副作用なし」と報告。
- A: `addMember()`のフォールバック（`group_members`が使えない環境向けの配列書き込み）に
  古典的な「読んでから書く」競合があり、2人がほぼ同時に参加すると片方が消える
  （HANDOFF自身が"直した"と説明している問題の再燃）
- B: 参加者が自分1人だけの状態で自分の回答を全部済ませても、状況バーがずっと
  「つぎにやること／このURLをグループに送る」のままで、アプリの核心である
  「このままだと〇日に決まります」という予告が永遠に出てこない

**選定と理由**：Aの指摘は実害が大きい一方、正しく直すには（真の原子性を持つDB操作が
無い状況で）楽観的排他制御＋リトライのような複雑なロジックが必要で、実機のSupabaseに
繋がずに正しさを検証しきる自信が持てなかった。対してBの指摘は`renderStatus()`の条件式
1つを直すだけの純粋なUI修正で、検証も容易・リスクも低く、かつ「作成者1人で使う」という
最も基本的な最初の利用シーンで100%再現する、アプリの一番の売りを裏切る不具合だった。
残りループ数を考え、確実に安全に直せるBを選んだ。

**直し方**：`renderStatus()`のソロホスト分岐の条件に`&& !hasAnswered(state.me)`を追加し、
既に回答済みなら予告表示の分岐に進むようにした。`tests/testsoloforecast.js`を追加。

**直さなかった指摘とその理由**：Aの`addMember()`フォールバック競合は、実害・再現性とも
高いことは認識しているが、正しい修正（比較後書き込みによる楽観的排他制御など）を
実機検証なしで安全に実装しきれる確信が持てず、次回以降の課題として明示的に見送った。
**未解決のまま残っている。**

---

## ループ10（最終周）

**指摘**：Cはループ9の修正（`renderStatus()`）を検証し、問題なしと報告（招待導線は
topbarに常設のボタンが別途あるため、ソロホストが誘導を見失うこともないと確認）。
- A: 複数の指摘の中に、**`gotoGroup('${esc(g.id)}')`（「前に使ったグループ」一覧）に
  保存型XSSの疑い**があるという指摘があった。`esc()`はHTMLエンティティ化するが、
  `onclick`属性内のシングルクォート文脈ではブラウザが属性値をJSとしてコンパイルする前に
  HTMLエンティティをデコードするため、`&#39;`は結局`'`に戻り、JS文字列リテラルを
  抜け出せてしまう。Supabaseの公開anonキーと緩いRLS（`using(true) with check(true)`）に
  より、第三者が`groups`テーブルへ直接insertして任意のidを仕込める（HANDOFF 3章に
  既知の弱点として明記されている）ため、攻撃者が細工したidへのリンクを踏ませ、被害者の
  「前に使ったグループ」一覧経由でJSを実行させる、という経路が理論上成立する。
  他にも`membersTable`フラグの永続的な劣化、ホストの自己削除によるロック、
  `submitAnswer()`の競合など、過去周から繰り返し指摘されている項目があった
- B: 単独（グループなし）でイベントを作る導線がコード上は実装済みなのに、UIからは
  一切到達できない（`openNewGroup()`が常にグループ名の入力を要求する）

**選定と理由**：Aの指摘のうち、まず自分でコードと`esc()`の実装、HTML属性値の
デコード順序を追ってXSSの疑いを検証した（サブエージェントの解釈を鵜呑みにしなかった）。
実際にPlaywrightで攻撃ペイロードを仕込んで検証したところ、**修正前のコードでは注入した
JSが実際に実行されることを確認した**（下記参照）。データ破損系のバグを9周連続で
直してきたが、コード実行を許すバグは実害の性質がまったく異なり最も深刻であるため、
最終周として最優先で選んだ。Bの「単独イベント作成が不可能」という指摘は根が深く価値も
高いが、UIに新しい導線（ボタン・画面遷移）を追加する必要があり、「新機能の追加はしない」
というループの制約に抵触するリスクがあると判断し、見送った（LOOP_REPORT末尾に記録）。

**直し方**：`onclick="gotoGroup('${esc(g.id)}')"`を
`onclick="gotoGroup(${esc(JSON.stringify(g.id))})"`に変更。`JSON.stringify()`で
JS文字列リテラルとして安全な形にまず変換し、それを`esc()`でHTML属性値として安全な形に
包む、二重のエスケープにした。`tests/testxss.js`を追加し、攻撃ペイロード
（`x');window.__xss=true;//`）を仕込んで：
- 修正前：クリック時に実際に`window.__xss`が`true`になり、`gotoGroup`に渡った引数も
  `'x'`だけに切り詰められていた（＝文字列リテラルを抜け出していたことの直接的な証拠）
- 修正後：`window.__xss`は`false`のまま、`gotoGroup`には元の文字列がそのまま1つの
  引数として渡っていた

ことを確認した。

**捨てた変更／作業中に見つけた別の問題**：
このテストを書く過程で、**ループ9で追加した`tests/testsoloforecast.js`に安全上の不備が
あることを発見した**。他の全テストが必ず行っている
`p.route('**cdn.jsdelivr.net**', r => r.abort())`（本物のSupabaseライブラリを読み込ませない
処理）が抜けており、実際に本物のSupabaseプロジェクトへ接続しうる状態でpushされていた。
このためテストは実行のたびに結果が変わる不安定な状態になっていた（該当箇所は
`event_id`が`null`のまま`answers`テーブルへの書き込みを試みる形で、`answers`の主キーに
`event_id`が含まれる設計上おそらく制約違反で弾かれ実データの汚染には至っていないと
考えられるが、**本物のSupabaseへネットワーク接続自体は発生していた可能性が高く、
これは明確なルール違反である**。今回のループ10でこの不備に気づき、他のテストと同じ遮断
処理を追加した上で、テストの検証方法自体も見直した（`submitAnswer()`を経由させると
デモモードの「全員回答済みで自動即確定」という別機能と混ざってしまうため、
`renderStatus()`が見る`state`を直接組み立てて検証する形に書き換えた）。
修正後は5回連続で安定して同じ結果になることを確認し、かつ修正前のコードに対しては
正しく失敗することも確認した。このテストファイルの修正はループ10のコミットに含めている。

---

## 全体を通じて見つかった「テスト側の不具合」（ループ本体のバグではないが記録）

- ループ5：`tests/testjoinerr.js`の作成時、クリックの度に3秒ごとの定期同期を跨いでしまい、
  このテストが検証したい不具合とは無関係な別の既知バグ（`membersTable`フォールバック）が
  混ざってテスト結果が不安定になった。カレンダーの実クリックをやめ、候補日選択のロジック
  を直接関数呼び出しする形に変更し、かつテスト中だけ定期同期を止めることで解決した。
- ループ6：`tests/testeventswitch.js`の作成時、最初のテスト設計では毎回`persist()`を
  明示的に呼んでフィードの保存を確定させていたため、肝心の「保存が確定する前に別
  イベントへ切り替える」という再現条件そのものを消してしまっていた。`persist()`の
  明示呼び出しをやめ、自然に保留状態のまま画面遷移させる形に直して再現に成功した。
- ループ10：上記の`testsoloforecast.js`の件（本物のSupabaseへの接続漏れ）。

---

## 最終的に直さなかった指摘の一覧とその理由（省略なし）

以下は複数周にわたって指摘されたが、意図的に見送ったもの。いずれも「起きやすさ×実害」の
天秤で他の指摘に劣ったか、安全に直しきる確証が持てなかったもの。

| 指摘 | 見送った理由 |
|---|---|
| `pull()`のガードに残る狭い競合（`persist()`完了時代入が`eventId`を見ていない） | 発生条件が非常に狭く、既にループ6のガードでリスクは大幅に低減済み。`pull()`/`persist()`周りにこれ以上手を入れるリスクの方が高いと判断 |
| `submitAnswer()`と`pull()`の競合による回答の取りこぼし（締切間際） | 正しく直すには`state.answers`に対してもループ5相当の「保留編集」概念が要り、影響範囲の見積もりに時間がかかる |
| コピーボタン2つを素早く連打したときのトースト文言の競合 | データ損失を伴わない表示上の軽微な問題。発生条件も限定的 |
| `addMember()`のフォールバック（配列書き込み）に残る読み書き競合 | 正しい修正には楽観的排他制御＋リトライが必要で、実機のSupabaseに繋がず安全に検証しきる確信が持てなかった |
| 単独（グループなし）でイベントを作る導線がUIから到達不能 | 直すには新しいボタン・画面遷移の追加が要り、「新機能は追加しない」という制約に抵触するリスクがある |
| ホストが自分自身を設定パネルから削除でき、`state.host`が更新されないためグループ全体がロックされる | 起きやすさは低い（自分を消すという能動的操作が必要）が、直すなら「ホストを他人に譲る」導線の設計判断が要り、単純な1行修正の範囲を超える |
| `createGroup()`のinsert成功後に`addMember()`が失敗すると孤立した`groups`行が残る | 実害は限定的（孤立データが残るだけで、ユーザー体験上は「失敗しました、もう一度」で完結する）。DB上の孤立行を掃除する仕組みは今回のスコープ外 |
| `membersTable`フラグが一度の通信エラーで永久に`false`になり、セッション中ずっと危険な配列書き込みにフォールバックし続ける | 複数周にわたり繰り返し指摘された、実害の大きい既知の問題。正しく直すには「本当にテーブルが無い」場合と「単発の通信エラー」を区別する必要があり（例えばエラーコードで判定するなど）、実機のPostgRESTのエラー形式を確認せずに安全な判定条件を書ける確信が持てず見送った。**次に着手すべき最有力候補** |
| `saveAnswer()`／`saveGroup()`の`.error`未チェック | 同じ既知パターン。`saveGroup()`は再送機構もない。優先度は高いが、今回のループでは他の指摘が上回った |
| `state.seq`が永続化されず、リロードや別デバイスでの候補日追加がID衝突を起こしうる | 複数周で指摘されたが、リロード＋候補日6件以上という条件がやや狭く、他の指摘を優先した |
| `pullGroup()`が`tick()`を呼ばず、締切超過イベントがグループ画面で「あと0時間」のまま固まる | 表示上の不整合であり、個別イベントを開けば正しく確定処理が走るため実害は限定的 |
| デモ用の名前「りくと」がデフォルトの`state.me`としてハードコードされており、その名前が実在するグループでは初見の訪問者が既存メンバーとして紛れ込みうる | 発生には「その名前のメンバーが実在する」という特殊条件が要る |
| 全メンバーを削除すると`tick()`の自動確定が永久に無効化される | ホストが全員を消すという特殊な操作が前提 |
| その他、UIの分かりにくさ・文言の不整合（40件以上、`LOOP.md`実行中の各周のサブエージェント報告に記録）| 個々の実害は小さいものと判断し、より高い実害の指摘を優先した。詳細は各周の項を参照 |

---

## コミット一覧（1周1コミット）

```
26a1d69 グループ作成でDB書き込みが失敗しても参加したことになり…（ループ1）
a58cbc7 保存失敗の直後に同期が割り込むと…（ループ2の元となった`persist()`修正）
c3ecfd4 招待リンクを開いた直後に通信が一度だけ失敗すると…（ループ3）
b5679f2 グループ参加の保存に失敗しても参加したことになり…（ループ4）
851c9b6 保存失敗の直後に同期が割り込むと、編集した内容が…（ループ5）
276c34a イベントを切り替えて開き直すと、前に見ていた別イベントの…（ループ6）
5292bfc 招待URLのコピーが実は失敗していても…（ループ7）
f4104ee 確定した日程の開始・終了時刻を、確定後でも…（ループ8）
c3d0b9e 自分1人だけで回答を全部済ませても…（ループ9）
421e7e1 『前に使ったグループ』一覧のonclickで…保存型XSS…（ループ10）
```

（ログ上のメッセージは`git log --oneline`で確認できる完全な文言と若干省略が異なる場合が
あるので、正確な文言は`git log`を参照）

## 次にこの続きをやる人へ

- `membersTable`フラグの恒久劣化（表の最有力候補）と`saveAnswer()`/`saveGroup()`の
  `.error`未チェックは、パターンとしては既に9回繰り返し修正してきたものと同じなので、
  同じ要領で直せるはず。
- `submitAnswer()`と`pull()`の競合、`persist()`完了時代入の`eventId`未スコープは、
  `pull()`/`persist()`/`lastRow`という一番デリケートな箇所に触るので、次に手を入れる際は
  Agent Cの役割（前周diff監査）を必ず一緒に回して、意図せぬ回帰が無いか確認すること。
  実際にループ6ではこの箇所の修正が新しい回帰を生んでいる。
- 単独イベント作成のUI導線復活は、実害というより機能面の欠落（コードはあるのに使えない）
  なので、修正専用のこのループの枠組みより、機能追加として別途検討した方がよい。
