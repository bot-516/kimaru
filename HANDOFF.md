# キマる — 引き継ぎ資料

この文書は、別のセッション（cmdのClaude Codeなど）がこのプロジェクトを引き継いで
作業を続けるために書かれている。**作業を始める前に、これを最後まで読むこと。**

---

## 1. これは何か

JPHACKS2026 の個人課題として作った日程調整ツール。作者は けんじ（GitHub: bot-516）。
プログラミングは初学者で、ターミナルにはまだ慣れていない。**説明は日本語で、専門用語は
使うときに一度かみ砕くこと。**

- 公開URL: https://bot-516.github.io/kimaru/
- リポジトリ: https://github.com/bot-516/kimaru （main ブランチ直下の `index.html` 1枚）
- データ: Supabase（プロジェクトref `syswwhswumqcfxdwjoiz`）

### 競合との違い（この一点を壊さないこと）

調整さんは「予定を**集める**」。表を出して、決めるのは人間。
キマるは「予定を**決める**」。締切が来たら自動で日が確定する。**催促する人が要らない。**

この主張は画面に常時出ている：「このままだと 8月26日(水) に決まります／あと18時間」。
機能を足すときも消すときも、この一文が成立しているかを基準にする。

---

## 2. 構成

**単一のHTMLファイル。ビルド無し、npm無し、フレームワーク無し。**
作者がVS Codeしか持っていなかった時期に決めた制約で、いまも維持している。
勝手にReact化やファイル分割をしないこと。

```
index.html   ← 全部これ1枚（HTML + CSS + JS、約105KB）
```

Supabase だけ CDN から読む：`https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2`

### 2つのモード

| mode | 条件 | 挙動 |
|---|---|---|
| `shared` | `SUPABASE.url` と `key` が入っている | 本番。Supabaseと同期 |
| `demo` | 空、または接続失敗 | 手元だけ。仮想時計で締切を早送りできる |

`SUPABASE` の値を空にすればデモに戻る。オフライン時の保険も兼ねている。

### 画面（`state.view`）

```
newgroup … 入口。グループを作る／前に使ったグループから戻る
group    … グループのホーム。メンバー・カレンダー・進行中の日程
new      … 日程を作るフォーム
cal      … 候補日カレンダー（点数で色分け、最有力に印）
board    … 集計表（誰が何と答えたか）
answer   … ○△×を入れる画面
feed     … 通知のタイムライン
```

URLの `#g=xxxx` がグループ、`#e=xxxx` がイベントを指す。ログインは無い。

---

## 3. データベース

```sql
groups        (id, name, members jsonb, created_at)
group_members (group_id, member, joined_at, PK(group_id, member))
events        (id, title, group_name, host, slot, place, memo, deadline,
               members jsonb, candidates jsonb, confirmed, failed,
               feed jsonb, sent_reminders jsonb, group_id, created_at)
answers       (event_id, member, value jsonb, updated_at, PK(event_id, member))
```

**`groups.members` は過去の遺物。今の正は `group_members`。**
古いグループを開いたときだけ、アプリが自動で `group_members` へ移し替える。

`answers` と `group_members` が別テーブルなのには理由がある。→ 6章を読むこと。

### アクセス許可（RLS）

`using(true) with check(true)` の全許可。ログインが無いのでこうするしかない。
接続キーはページのソースに書いてあるので、**URLを知っていれば誰でも書き換えられる。**
これはデモ用途としての割り切りで、作者もそう説明できる状態にしてある。
（`supabase_harden.sql` を適用済みなら、削除だけはできない）

---

## 4. 決まり方

```
○ = +2点   △ = +1点   × = −3点
```

- 合計が一番高い日が選ばれる。同点なら早い日。
- **合計0点以下の日は選ばれない。**カレンダーで取り消し線が引かれ、確定もできない。
- 全部の候補日が0点以下なら「不成立」。候補日を足して再調整する。

確定のタイミングは「締切を過ぎたあと、最初に誰かがページを開いた時」。
常駐サーバーが無いので、そうするしかない。画面にもそう書いてある（隠していない）。

---

## 5. 絶対に壊してはいけない決まりごと

コードを変えるときは、以下に反していないか毎回確認すること。

1. **Secret key を絶対にファイルに書かない。** Publishable key だけ。
   （`sb_publishable_...` はOK。`service_role` や `sb_secret_...` は論外）
2. **取り消し線を引く条件と、確定できない条件は同じ式（`score<=0`）。**
   別々にすると「無理と線を引いた日が確定する」という矛盾が起きる。実際に起きた。
3. **未送信の回答（`state.draft`）は集計に混ぜない。** 送信ボタンを押すまでは誰の目にも入らない。
4. **メンバーの追加は `group_members` に1行足すだけ。** 名簿を配列で丸ごと書かない。
5. **`persist()` は変わった項目だけ書く。** 行を丸ごと上書きしない。
6. **`render()` は指が触れている間（`uiBusy`）は走らせない。** 押している最中にDOMを捨てるとクリックが消える。
7. **同期は中身が変わったときだけ再描画する。** 無変化で3秒ごとに作り直さない。
8. **保存に失敗したら「できました」と言わない。** 画面に留めて、入力を残して、理由を出す。
9. **デザインの決まり**：絵文字を使わない。グラデーションを使わない。大きい角丸を使わない。
   朱色（`--accent:#b0421f`）は「決まる／決まった」にだけ使う。見出しは明朝体。
   これは「AIが作った感じ」を避けるために意図的にそろえてある。崩さないこと。
10. **説明文を増やさない。** 作者から「文章が長い」と何度も指摘されている。
    見出しの下に説明をぶら下げる書き方は避け、画面で分かるようにする。

---

## 6. 過去に踏んだ罠（同じ穴に落ちないため）

| 症状 | 原因 | 直し方 |
|---|---|---|
| 同時に参加した人が消える | 名簿を配列で丸ごと保存していた。後から保存した人が前の人ごと上書き | `group_members` に1人1行 |
| あとから誘った人が回答できない | イベント作成時の名簿をコピーして固定していた | グループの日程は、常に今のグループの名簿を使う |
| ボタンの1回目が効かない | 3秒ごとの同期がDOMを作り直し、押し下げ〜離すの間にボタンが消えていた | `uiBusy` 中は描き直さない。離した直後も1手番あけてから（clickが届く前に消さないため） |
| 名前を入力中に消える | 同期のたびに入力欄ごと作り直していた | `renderBody()` でフォーカスと値を復元 |
| 作成に失敗したのに「できました」 | catchでトーストを出したあと成功処理に流れていた | 失敗したら return。前のトーストも消す |
| 失敗後にもう一度押すとクラッシュ | 通信より先に入力欄の状態を捨てていた | 成功してから捨てる |
| セルの文字が読めない | 半透明の背景が暗い下地と重なっていた | `heat()` で不透明なRGBに焼き込む |
| カレンダー最終行が黒い塊になる | 7列に満たない行の下地が見えていた | 空セルで埋める |
| 「あと0時間」と出る | `Math.round` で20分が0時間になっていた | 2時間未満は分で出す |
| 名前にアポストロフィがあると壊れる | `onclick="f('${name}')"` に名前を埋めていた | インデックス渡しにする |
| URLを手で書き換えると画面が混ざる | ハッシュの変化を検知していなかった | `hashchange` で読み込み直す |

---

## 7. テストの回し方

`tests/` に9本ある。Playwright を使う。

```bash
npm install playwright
npx playwright install chromium
node tests/audit.js        # 自動採点。51/51 が満点。ここが下がったら何かを壊している
node tests/bugcheck.js     # 主要な不具合の再発チェック
node tests/testclick.js    # 同期がクリックを飲み込まないか
node tests/testfail.js     # 通信が失敗したときに嘘をつかないか・壊れないか
node tests/testrace.js     # 同時参加でメンバーが消えないか
node tests/testreport.js   # 後から入った人が回答できるか
node tests/testgroup.js    # グループ機能ひととおり
node tests/testgcal.js     # グループのカレンダーから作る流れ
node tests/testrecent.js   # 前に使ったグループを覚えているか
node tests/testfallback.js # group_members が無い環境でも動くか
```

各テストは**本物のSupabaseに繋がない。**冒頭で `cdn.jsdelivr.net` への通信を遮断し、
本物のライブラリを読ませないうえで、同じ形をした偽サーバをページに差し込んで動かす。
**この遮断を外すと本物のDBにテストデータが書き込まれる。絶対に外さないこと。**
だから何度回してもデータは汚れないし、ネットワークも要らない。

`RUBRIC.md` に評価基準（100点満点）がある。合格ラインは各カテゴリ16以上かつ合計88以上。

---

## 8. 変更をGitHubに反映する

リポジトリを clone してあるなら：

```bash
git add index.html
git commit -m "何を直したか（日本語でよい）"
git push
```

反映まで1〜2分。https://bot-516.github.io/kimaru/ で確認できる。

**注意**：作者は以前、ブラウザから手作業でアップロードしていた。その名残で
`index (1).html` のようなファイルが混ざった事故がある。ファイル名は必ず `index.html`。

---

## 9. 残っている課題

- **同じ項目を同時に書き換えた場合は、あとから書いた方が残る。**
  項目ごとの差分更新までは入れたが、バージョンチェックは入れていない。
- **サーバー側の権限検証が無い。** `isHost()` は見た目の制御だけ。
  本気で守るには Supabase Auth を入れて、締切と候補日の変更をサーバ側で判定する必要がある。
- **リーダーのアプリ（スキマ）にあってキマるに無いもの**：
  Googleカレンダー連携／日だけでなく時間帯まで決める／未回答者への実際の通知送信。
  通知は「送信機能はありません」と画面に明記してある。嘘をつかないこと。

---

## 10. 作者とのやりとりで気をつけること

- 「AIが作った感じ」を強く嫌っている。画一的なUIと長い説明文は嫌われる。
- 事実と推測を混ぜない。確認していないことは「確認していない」と言う。
  過去に一度、原因を推測で断定して外し、あとで訂正している。
- 手を動かす前に、何をどう直すかを短く言う。実装したら、実測した確認結果を出す。
- 褒めるためだけの前置きを書かない。
